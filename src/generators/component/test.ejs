import <%= _.upperFirst(_.camelCase(name)) %> from '../../../../admin/components/ui/<%= _.snakeCase(name) %>'
import { shallow } from 'enzyme'
import { expect } from 'chai'
import { spy } from 'sinon'
import React from 'react'

describe('<%= _.snakeCase(name) %> component', () => {

})

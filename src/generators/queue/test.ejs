<% capitalized = _.upperFirst(_.camelCase(name)) -%>
import { expect } from 'chai'
import <%= capitalized %>Queue from '../../queues/<%= _.snakeCase(name) %>'

describe('<%= _.snakeCase(name) %> queue', () => {

})

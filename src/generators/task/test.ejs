<% capitalized = _.upperFirst(_.camelCase(name)) -%>
import { expect } from 'chai'
import <%= capitalized %> from '../../tasks/<%= _.snakeCase(name) %>'

describe('<%= _.snakeCase(name) %> task', () => {

})

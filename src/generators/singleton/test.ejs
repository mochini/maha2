import React from 'react'
import { expect } from 'chai'
import { shallow } from 'enzyme'
import { spy } from 'sinon'
import * as actions from './actions'
import * as selectors from './selectors'
import reducer from './reducer'
import <%= _.upperFirst(_.camelCase(name)) %> from '../../admin/components/<%= _.snakeCase(name) %>/<%= _.snakeCase(name) %>'

describe('<%= _.snakeCase(name) %> component', () => {

  describe('actions', () => {

  })

  describe('reducer', () => {

  })

  describe('selectors', () => {

  })

  describe('component', () => {

  })

})
